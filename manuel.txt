	setup the Raspberry:

https://www.youtube.com/watch?v=Boi4cxHFKwg



	install opencv:

sudo apt-get update

sudo apt-get install build-essential cmake pkg-config libjpeg-dev libtiff5-dev libjasper-dev libpng-dev libavcodec-dev libavformat-dev libswscale-dev libv4l-dev libxvidcore-dev libx264-dev libfontconfig1-dev libcairo2-dev libgdk-pixbuf2.0-dev libpango1.0-dev libgtk2.0-dev libgtk-3-dev libatlas-base-dev gfortran libhdf5-dev libhdf5-serial-dev libhdf5-103 python3-pyqt5 python3-dev -y

pip install opencv-python==4.5.3.56

pip install -U numpy


	activate legacy camera:

sudo raspi-config	 
--> 3. option --> first option --> navigate finish


	install Autocam:

git clone https://github.com/nanocraftmr/Autocam.git

cd Autocam

python3 autocam.py

sudo chmod 777 /home/pi/Desktop/images/

	setup webserver:

sudo apt-get install apache2
sudo apt-get install php
sudo chmod -R 755 /home/pi/Desktop/images/
sudo ln -s /home/pi/Desktop/images /var/www/html/autocamserver/images

#add own icon to website:
cd /var/www/html/autocamserver/
here paste in your logo.png file








